const usersRepository = require('./users');
const postsRepository = require('./posts');
const answersRepository = require('./answers');
const commentsRepository = require('./comments');
const tagsRepository = require('./tags');
const dashboardRepository = require('./dashboard');

module.exports = {
  usersRepository,
  postsRepository,
  answersRepository,
  commentsRepository,
  tagsRepository,
  dashboardRepository
};
